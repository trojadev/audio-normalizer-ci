Audio Alchemist CLI

A Python-based utility designed to automate processing .wav audio files using the SoX (Sound eXchange) library. This tool processes audio files adding bunch of audio FX with random parameters and generates a JSON-formatted summary of the processed files, including duration and channel configuration.

By default program outputs 3 processed variations of source file.