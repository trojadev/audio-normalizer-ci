name: Audio Pipeline

on:
  push:
    paths-ignore:
      - 'README.md'
      

jobs:
  build-image:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12","3.13"]
    steps:

      - name: Set up Docker
        uses: docker/setup-docker-action@v5
        with:
          daemon-config: |
            {
              "debug": true,
              "features": { 
                "containerd-snapshotter": true
              }
            }
            
      - name: Download code from repo
        uses: actions/checkout@v6
      
      
      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
      
      - name: Extract Metadata for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha,format=short,prefix=,
            type=raw, value= ${{matrix.python-version}}
            type=raw, value=latest
            
            

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          build-args: |
            PYTHON_VER=${{matrix.python-version}}
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha, mode=max


      - run: docker run --rm -u $(id -u):$(id -g) -v $(pwd)/data:/data ghcr.io/${{ github.repository }}:latest #/data/essa.wav   


      - name: Save processed files
        uses: actions/upload-artifact@v6
        with:
          name: audio_normalized_python_${{ matrix.python-version }}
          path: tests/processed_files/